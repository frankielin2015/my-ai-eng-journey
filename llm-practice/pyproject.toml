[project]
name = "llm-practice"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Xiaofa Lin", email = "frankielin2015@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "openai>=3.3.1",
    "python-dotenv>=1.2.3",
    "psycopg2-binary>=2.9.10",
    "pgvector>=0.4.1",
]

[project.scripts]
llm-practice = "llm_practice:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = []
